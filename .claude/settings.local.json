{
  "permissions": {
    "allow": [
      "Bash(nvidia-smi --query-gpu=name,memory.total --format=csv,noheader 2>/dev/null || echo \"No NVIDIA GPU detected in WSL\")",
      "Bash(which uv:*)"
    ]
  }
}
